import { TogglepassDirective } from './togglepass.directive';

describe('TogglepassDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglepassDirective();
    expect(directive).toBeTruthy();
  });
});
